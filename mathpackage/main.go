package main

import (
	"fmt"
	"math"
)

/**
math package
- float64 is the data type used by math package function
*/

func main() {
	fmt.Printf("PI: %f \n", math.Pi)
	fmt.Printf("SQRT: %f \n", math.Sqrt(3))
	fmt.Printf("Abs: %f \n", math.Abs(-2.3452))
}
